{
  "id": "9c500a1d",
  "title": "Desktop Sidecar package (WebSocket + auth + Bun probe)",
  "tags": [
    "checkpoint-3"
  ],
  "status": "open",
  "created_at": "2026-06-01T14:24:52.177Z"
}

Create packages/desktop-sidecar: WebSocket server bound to 127.0.0.1 with random port + token auth + Origin check. Hosts the headless DesktopCore. Exposes CLI entry that loads pi-sdk-driver, runs bun:probe to validate bun runtime, falls back to node binary. Implements lifecycle: Tauri passes auth token via env, sidecar mints port-file with token+port.
