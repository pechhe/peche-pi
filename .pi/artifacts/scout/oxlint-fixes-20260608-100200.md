# Context for: Fix oxlint warnings in apps/desktop/src

## Relevant Files
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/.oxlintrc.json` — project's oxlint config (only react-hooks rules + ignores tests/out/dist/node_modules)
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/icons.tsx` — removed 4 unused icon functions + 3 orphaned fragments from incomplete removals
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/surfaces/utility-surface.tsx` — removed unused `CavemanLevel` import
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/automations-view.tsx` — removed unused `AUTOMATION_PRESET_CRON` import
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/sidebar.tsx` — removed unused `automations` destructure
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/hooks/use-keyboard-shortcuts.ts` — fixed unused expression (ternary → if-else)
- `/Users/admin/Documents/2. coding projects.nosync/peche-pi/apps/desktop/src/App.tsx` — removed unused type imports + fixed 3 exhaustive-deps warnings

## Project Structure
- apps/desktop/ — Electron app with src/ (renderer), electron/ (main process), tests/ (Playwright)
- Project's oxlint config is at apps/desktop/.oxlintrc.json with minimal rules:
  - react-hooks/rules-of-hooks: error
  - react-hooks/exhaustive-deps: warn
  - ignorePatterns: out/**, dist/**, node_modules/**, release/**, tests/**

## Existing Patterns
- Lint script: `"lint": "npx oxlint src/"` (in apps/desktop/package.json)
- No lint command in root package.json for oxlint
- Run from apps/desktop directory so .oxlintrc.json is discovered

## Dependencies
- oxlint v1.68.0 (via npx)
- No ESLint config or eslintrc files in the project

## Key Findings
- **Project runs oxlint with very conservative config** — only react-hooks rules active
- **Running with `-D all` produces 15,411 issues** across 153 files — mostly style rules like no-magic-numbers, sort-keys, no-ternary, func-style, explicit-function-return-type
- **These style rules are intentionally suppressed** by the project's minimal .oxlintrc.json
- **The real issues were all warnings** — 10 no-unused-vars, 1 no-unused-expressions, 3 exhaustive-deps
- **icons.tsx had the most issues** — 4 unused exported functions + incomplete function removals created orphaned code fragments

## Gotchas
- oxlint with `-D all` is WAY too noisy for this project (15K+ issues). The project deliberately uses a minimal config.
- Initial oxlint runs found "No files found to lint" because .oxlintrc.json's ignorePatterns and the project's .gitignore combined with the config path resolution issue (running from wrong directory). Must run from apps/desktop/ or use -c flag.
- The `edit` tool's partial replacements on icons.tsx left orphaned SVG path fragments that caused syntax errors. Had to clean up manually.

## Answer
Fixed all actual oxlint issues (project config). Before: 10 warnings + 3 exhaustive-deps warnings. After: clean.

### Changes made:

1. **icons.tsx** — Removed 4 unused exported functions (`ArchiveIcon`, `PiGlyphIcon`, `GripIcon`, `QueueIcon`) and cleaned up 3 orphaned code fragments from partial removals.
2. **utility-surface.tsx** — Removed unused `CavemanLevel` type import.
3. **automations-view.tsx** — Removed unused `AUTOMATION_PRESET_CRON` import.
4. **sidebar.tsx** — Removed unused `automations` variable from destructure.
5. **use-keyboard-shortcuts.ts** — Changed `threadSearch.isOpen ? close() : open()` ternary expression to if-else.
6. **App.tsx** — Removed unused `ComposerAttachment` and `NewThreadEnvironment` type imports. Fixed 3 exhaustive-deps warnings: added missing deps `setNewThreadComposerMode`, `setNewThreadRootWorkspaceId`, `setNewThreadEnvironment`, `setPendingNewThreadWorkspaceId`, `clearAllDrafts`.

## Next Steps
Ready to proceed — no follow-up needed. Run `cd apps/desktop && npx oxlint src/` to verify clean state.
