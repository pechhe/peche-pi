{
  "version": 1,
  "runtime_contract": {
    "source_docs": [],
    "verification_gates": [
      { "name": "verify file exists", "command": "test -f test.txt && grep -q '^test$' test.txt" }
    ],
    "require_progress_append": true,
    "require_one_item_per_iteration": true,
    "require_commit": true
  },
  "items": [
    {
      "category": "functional",
      "description": "Create test.txt with content 'test' and commit it",
      "steps": [
        "Initialize git repo at workspace root if missing",
        "Write test.txt containing exactly 'test'",
        "Stage and commit test.txt",
        "Verify file exists and content is correct"
      ],
      "passes": false,
      "regression_notes": ""
    }
  ]
}
